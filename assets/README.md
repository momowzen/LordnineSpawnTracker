assets/
This folder holds static assets used by the project (images, audio, sprites, etc).

Place project assets here and reference them from code. Keep the .gitkeep file so the directory is preserved in the repository when empty.
